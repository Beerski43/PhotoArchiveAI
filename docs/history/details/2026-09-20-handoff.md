# 申し送り（2026-09-20）

セッションを切る時点の状態。**新しいセッションはここを読めば続きから動ける。**

## いまどこにいるか

**Phase 3（#35）の手順2**。全件スキャンは終わり、GUI での手動割り当てを
始めたところ（8件）。ここは人手の作業で、進まないと手順3以降は測れない。

| 見たもの | 値 |
|---|---|
| `Media` | 70,297 件（**未スキャン 0**） |
| 顔 | 58,606 件 |
| 人物 | 5 名 |
| 手動割り当て | 8 件 |
| スキーマ版 | **2**（`data/photoarchive.db`、359MB） |

数字は古くなる。`docs/plan/phase-3-accuracy.md` に数え直すコマンドを置いた。

## 途中の作業: #48（人物の誕生日）

> **2026-09-20 追記: 解消した。** `develop` へ rebase して GUI まで仕上げ、
> PR を起票した。以下は当時の状態の記録として残す。**残っているのは
> マージ後の実DBの移行だけ**（下の ⚠️）。

**ブランチ `feature/#48_person-birth-date` にコミット済み・push 済み。
PR はまだ起票していない。**

| 部分 | 状態 |
|---|---|
| `Person.birth_date` の追加（スキーマ版 2 → 3） | **完了**（`4ef3ba1`） |
| 移行を版ごとに分岐（v2 以降は `ALTER TABLE` のみ） | **完了**。テスト7件つき |
| GUI（誕生日の入力欄、プレビューに年齢、年齢ダイアログの初期値） | ~~未着手~~ → **完了** |
| 仕様書 §7.2 / §7.7 / §8.4 | 完了（GUI 側の記述は入っている） |
| `GUI_USAGE.md` | ~~未着手~~ → **完了** |

### なぜ途中で止まっていたか

GUI 部分が PR #38（プレビューの情報欄）と同じ場所を触るため、**#38 のマージ待ち
だった**。#38 は 2026-09-20 にマージ済みなので、`develop` へ rebase して続けた
（WORKLOG は #48 側で触っていなかったので、衝突は起きなかった）。

### 決まっている仕様（Issue #48 のコメントに記録あり）

- 撮影日が誕生日より前なら **「誕生前」と出す**（行を消さない。人物の選び間違いに
  気づけるようにするため）
- **年齢ダイアログの初期値に計算値を入れる。** ただし**自動保存はしない**
- 誕生日は **年月日を必須**とする（年だけ・年月だけは持たない）

### ⚠️ 実DBは「版だけ版3」の状態にある。移行が要る

**2026-09-20 追記。** `data/photoarchive.db` は `user_version` が **3 なのに
`Person.birth_date` が無い**。`db.create_tables` が、移行していないDBに版の印だけを
刻んでいたため（#48 で直した。WORKLOG 先頭のエントリを参照）。

**この状態のままでは CLI は開かない**（開くと移行を促して止まる）。
直し方は2つ。どちらもバックアップを自動で取り、何も破棄しない。

**GUI から**（#52。いちばん手数が少ない）

```bash
photoarchive-gui        # 起動すると確認が出る → [移行を実行] を押す
```

**端末から**

```bash
photoarchive migrate --db data/photoarchive.db --yes
```

実データの複製で確認済み。CLI で1秒、GUI 経由で 2.4 秒（バックアップ込み）、
いずれも無損失で `integrity_check` も ok。

## ⚠️ マージしたら実DBの移行が要る

**#48 が `develop` に入るとスキーマ版が 3 になり、実データ（現在 2）は
`photoarchive migrate` を実行するまで他のコマンドが止まる。** GUI も同じ。

移行は列を1本足すだけで**何も破棄しない**。実データの複製（359MB / 顔 58,606 件）で
**2.5秒・無損失**を確認済み。バックアップも自動で取られる。

```bash
photoarchive migrate --db data/photoarchive.db
```

## 次の一手

1. **#48 の PR をレビューしてマージする。** マージしたら実DBの移行（下の ⚠️）
2. **手順2を進める**（GUI で人物ごとに20〜50枚）。`photoarchive-gui`。
   誕生日を登録しておくと、年齢が初期値で入るので入力が速い
3. 手本が **1人につき別の写真から2枚以上**入ったら `photoarchive evaluate` が動く

## 判断が残っているもの

| 何 | どこ |
|---|---|
| `data/photoarchive.db.bak-pre28`（739MB）を消すか | Phase 3 手順7。全件スキャンが無事終わったので判断できる |
| `selection.py` の `family_only` の扱い | Phase 3 手順5。閾値が決まってから |
| `Person.name` の UNIQUE 制約 | Phase 3 手順6 |
| 設定の相対パスを設定ファイル基準に揃えるか | #38 のレビューで保留。`database_path` は cwd 依存のまま。Phase 5（#27）の範囲 |

## 環境の状態（作業に効くもの）

- **`scan` のワーカーは `spawn`。** `fork` に戻すとDBが壊れる（#39）
- **GitHub はマージ時に head ブランチを自動削除**する設定にした。ローカルは
  `fetch.prune = true`
- 実データは NFS（`/mnt/nfs/nanoPi-NEO2/`）。`mediaFiles/` の symlink 経由
- **`photo_natsu` 側（5,323件）も スキャン済み**。ただし設定の `source_root` は
  `suzukiFamily` だけなので、`scan` を無引数で流すと `photo_natsu` は対象外
