# PhotoArchiveAI — AIエージェント向け作業ルール

**このファイルが作業ルールの正本。** `.github/copilot-instructions.md` は
ここへのポインタで、内容はこちらにある。

---

## 1. まず読むもの

作業を始める前に、現在のブランチと作業ツリーを確認したうえで、この順に読む。

| # | ファイル | 何が分かるか |
|---|---|---|
| 1 | `docs/plan/ROADMAP.md` | 今どのフェーズか、次に何をやるか |
| 2 | `docs/history/WORKLOG.md` | 直近に何をやったか（**先頭が最新**） |
| 3 | `docs/spec/Specification.md` | 要件・仕様・DB設計・CLIリファレンス |
| 4 | `README.md` | セットアップ、依存関係、基本操作 |
| 5 | `docs/testing/TEST_CASES.md` | 何がテストで守られているか |

対象ファイルが無い場合は、無いことを確認したうえで読めるものだけを読む。

**ブランチごとに文書の配置も内容も違う。** 別ブランチで読んだ記憶を、
現在のファイルの代わりに使わないこと。必ず今のブランチのファイルを読む。

---

## 2. 「実装を再開して」と言われたとき

```
1. git fetch して、develop と現在のブランチの位置を確認する
2. docs/history/WORKLOG.md の先頭を読む      … 直近に何が終わったか
3. docs/plan/ROADMAP.md で進行中のフェーズを特定する
4. そのフェーズの docs/plan/phase-N-*.md を読む
5. 対応する Issue の未完了項目を確認する
6. 未着手なら、方針を Issue にコメントしてからブランチを切る
```

途中の作業が残っている場合は、`docs/history/details/` の最新の申し送りも読む。

---

## 3. 進め方（必ずこの順序で、PR起票で止まる）

1. **作業の単位は Issue。** 対象の Issue 番号を確認する。該当する Issue が
   なければ、まず Issue を作るか、ユーザーに確認する。
2. **実装前に、方針を Issue へコメントとして起票する。** 現状調査の結果、
   設計、影響範囲、検証手順を書く。記録を残すのが目的なので、実装が終わった
   あとに書くのでは意味がない。
3. **ブランチを切る。** `develop` から分岐する。
   - ブランチ名は `feature/#<Issue番号>_<英語のケバブケース>`
   - 例: `feature/#28_rework-scan-and-match`
   - **ブランチ名は英語で書く。** 日本語を含めない。
4. **実装する。** コミットメッセージの先頭は `#<Issue番号> `。
   例: `#28 scanに顔検出を統合`
5. **作業履歴を残す。** `docs/history/WORKLOG.md` の先頭に追記し、
   `python scripts/archive_worklog.py` を実行して、その差分も同じコミットに含める。
6. **回帰テストを実行する**（§5）。
7. **PR を起票したら、そこで止まる。**
   - PR のベースは `develop`。本文に回帰テストの結果を貼る
   - **レビューはユーザーが行う。** エージェントは PR をマージしない、
     `develop` / `main` へ直接 push しない、Issue をクローズしない。

> `main` ではなく `develop` が統合ブランチ。`main` は初期状態のまま止まっている。

### フェーズと実装プラン

- **新しいフェーズに入るときに、そのフェーズの Issue を起票し、
  `docs/plan/ROADMAP.md` に番号を書く。** 全フェーズの Issue を先に作らない。
- 着手済み・着手中のフェーズだけ `docs/plan/phase-N-*.md` に詳細を書く。
  未着手フェーズの詳細は陳腐化するので書かない。
- **レビューや通常の対話でプランを変えてよい。** 変えたら ROADMAP を同じ作業で
  更新し、WORKLOG に「プラン改版」として1行残す。

---

## 4. 実装の判断基準

- 仕様書の要求、README の利用方法、関連ドキュメントの説明が食い違っている場合は、
  **実装前に差分を整理する。** どれが正かを決めずにコードを書かない。
- 既存の実装・DBスキーマ・テストを確認し、**仕様と現状のどちらを直すべきか**を
  判断する。現状に合わせて仕様を書き換えるのか、仕様どおりに実装を直すのかを
  明示する。
- **ユーザーが明示的に求めていない仕様や公開APIを勝手に変えない。**
- **DBスキーマを変更する場合は、既存DBへの影響と移行方法を必ず確認する。**
  このリポジトリの実データは70,000件規模で、作り直しは現実的でないことがある。
  移行は `photoarchive migrate` に実装し、`PRAGMA user_version` で版を管理する。
- CLIの引数、GUI操作、ファイル形式、依存ライブラリを変更したら、
  **README または関連ドキュメントを同じ作業で更新する。**
- 既存ドキュメントが書いているコマンド、パス、テーブル名、画面名と、
  実装を一致させる。
- **修正1件につきテストを1件以上足す。** テストの無い修正は入れない。

---

## 5. 回帰テスト（PR起票前に必ず実行する）

```bash
cd /home/suu/github/PhotoArchiveAI && source .venv/bin/activate
./scripts/run_regression.sh
```

- 最終行の `回帰テスト: N passed / M failed (X.XXs)` を **PR 本文に貼る**
- **落ちた状態で PR を起票しない**
- 実行できない場合は、理由と代替の確認方法を PR に書く

個別に動かす場合。

```bash
pytest -q                  # 全テスト
pytest -q -m system        # 通しテストのみ
pytest -q -m models        # 実物の dlib モデルが要るテスト(環境依存)
pytest -q -m "not models"  # 回帰テストと同じ範囲
```

`-m models` は実物の学習済みモデルがある環境でしか走らないので、
**回帰テストの合否には含めない。**

実行時間は全110件で2〜3秒。**回帰テストは繰り返し実行するものなので、
この規模を保つ。** 詳細は `docs/testing/TESTING.md`。

### 実装後に確認すること

1. 仕様書と関連ドキュメントの要求を満たしているか
2. README の手順・コマンド・依存関係が実装と一致しているか
3. 必要なドキュメントを更新したか（仕様が変わったなら `docs/spec/Specification.md` も）
4. 回帰テストが通るか
5. 静的エラー、差分の空白エラー、不要な変更がないか

仕様やドキュメントに未解決の矛盾が残る場合は、**完了扱いにせず**
`docs/history/details/<日付>-<英語のケバブケース>.md` に申し送りとして書き、
WORKLOG からリンクする。

---

## 6. ファイルの置き場所

| 置き場所 | 何を置くか | git |
|---|---|---|
| `src/photoarchive_ai/` | アプリ本体 | 管理 |
| `tests/` | テスト。**`tmp_path` を使い、リポジトリ内に書かない** | 管理 |
| `scripts/` | 開発用スクリプト（回帰テスト、履歴の切り出し） | 管理 |
| `config/` | `*.sample.json` のみ管理。実際の設定は管理外 | 一部 |
| `docs/spec/` | 要件・仕様・DB設計 | 管理 |
| `docs/plan/` | 実装プラン（`ROADMAP.md` とフェーズ別） | 管理 |
| `docs/history/` | 作業履歴（`WORKLOG.md`、`archive/`、`details/`） | 管理 |
| `docs/testing/` | テスト環境・実行手順・テスト項目一覧 | 管理 |
| `docs/operation/` | 利用者向けの操作手順 | 管理 |
| `data/` | DB・ログ・バックアップ。生成物 | 管理外 |
| `mediaFiles/` | 実データへの symlink 置き場 | 管理外 |
| `models/` | dlib の `.dat` を手置きする場合の探索先 | 管理外 |
| `output/` | `select` の既定のコピー先 | 管理外 |

**ビルド生成物（`*.egg-info/`、`build/`、`dist/`）を git に入れない。**
`pip install -e .` のたびに再生成されるもので、ソースではない。

`docs/` の直下に文書を置かない。索引は `docs/README.md`。

---

## 7. このリポジトリの構成

### 処理の流れ（Issue #28 以降）

```
scan          パスの登録 + 顔検出 + 顔画像/特徴量/スコアの保存 + 消えた行の削除
  ↓
photoarchive-gui   人物の登録 + 検出済みの顔を人物へ手動で割り当て
  ↓
match         手動割り当てを手本に、残りの顔を自動で紐づけ
  ↓
select        ルールに従って抽出しコピー
```

**この順序が設計の核心。** 人物を登録する前に自動紐づけを走らせてはいけない。
旧 `analyze` はこの3つを一手に引き受けていて順序が壊れていたため、廃止した。

### モジュール

| ファイル | 役割 |
|---|---|
| `db.py` | スキーマと永続化。スキーマ版は `SCHEMA_VERSION` |
| `migration.py` | 旧スキーマからの移行（テーブル再構築方式） |
| `scanner.py` | 走査・差分判定・顔検出の呼び出し・消えた行の削除 |
| `face.py` | 顔検出(MediaPipe)と顔特徴量(dlib)。**矩形の正規化は `face_rect` に集約** |
| `scoring.py` | 笑顔・画質のスコア（FaceMesh） |
| `matcher.py` | 自動紐づけと `family_score` の再計算 |
| `gui.py` | 人物登録と顔の割り当て画面 |
| `cli.py` | サブコマンド定義 |
| `selection.py` | ルールに基づく抽出とコピー |
| `converter.py` | HEIC/HEIF → JPEG 変換 |
| `config.py` | 設定ファイルの探索と読み込み |

### DBの要点

- `Media.face_count`: **NULL=未スキャン / 0=顔なし / N=検出数**。この3状態の
  区別が差分スキャンの土台なので、勝手に 0 で埋めない。
- `Face.assign_source`: `NULL`=未割当 / `'manual'`=手動 / `'auto'`=自動 /
  `'rejected'`=除外。**手本にしてよいのは `'manual'` だけ。**
- `Face.embedding`: float32×128 のBLOB。小さすぎる顔は `NULL`。
- `Face.bbox_*`: **検出器が返した生の矩形。** dlib に渡す正規化後の矩形ではない。
- `AnalysisResult`: `smile_score` / `quality_score` は `scan` が、
  `family_score` は `match` が書く。互いに潰さないこと（UPSERTを使う）。

### 用語

同じものを別の名前で呼ばない。**「手本」「割り当て」「除外」「確定」「未割当」**
に揃える（「教師データ」「紐づけ」などをコードに持ち込まない）。
一覧は `docs/spec/Specification.md` の §1.3。

---

## 8. 踏みやすい落とし穴

- **`face_recognition` パッケージを import しない。** `face_recognition_models`
  の `__init__.py` が `pkg_resources` に依存しており、setuptools 81 以降では
  `ModuleNotFoundError` になる（Issue #10 の原因）。モデルのパスは
  `importlib.util.find_spec` で取り出す（モジュールを実行しないので踏まない）。
- **顔の矩形の正規化規約を変えない。** パディングの取り方を 0% → 25% に
  変えるだけで特徴量の距離が 0.03〜0.57 動く（別人判定の閾値と同じオーダー）。
  変える場合は `face.EMBED_VERSION` を上げて再スキャン対象にすること。
- **顔の一覧を全件読まない。** サムネイルのBLOBを全件読むと数百MBになり、
  GUIが固まる。必ず `LIMIT` / `OFFSET` でページ単位に読む。
  **ページャの無い一覧を作らない。**
- **メディアは `source_root` が NFS 上にある。** 読み出しが律速（実測24MB/s）。
  同じファイルをハッシュ用とデコード用で二度読みしない。
  **ハッシュが同じでもファイル属性は書き戻す。** 書き戻さないと、更新時刻だけ
  変わったファイルを毎回全部読み直すことになる。
- **消えたファイルの削除には安全弁がある。** 未マウント時の全消しを防ぐため、
  削除対象が2割を超えると中断する。この安全弁を外さない。
- **`face.get_latest_error()` は大域変数。** 1ファイルの処理を始めるときに
  `clear_latest_error()` で消さないと、前のファイルのエラーが次に付く。
  並列実行では fork 時の値が全ワーカーに複製される。
- **`db.assign_faces` の `age` は `KEEP_AGE` が既定。** `None` は
  「未設定に戻す」という指示であって「触らない」ではない。
- テストの `mediapipe` フェイクは `relative_bounding_box` を返す。実装が先に
  見るのがこの属性だから。`bounding_box` だけを返すフェイクに戻さない。
- **テストはリポジトリの中にファイルを書かない。** `tmp_path` を使う。
